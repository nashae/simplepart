{% extends "base.html.twig" %}

{% block title %}{{blog.title}}{% endblock %}

{% block body %}
<div class="container">
<div class="jumbotron">
  <h1 class="display-5" align="center">{{blog.title}}</h1>
  <hr class="my-4">
  <p>par auteur le {{blog.createdAt|date('d/m/Y') }}</p>
</div>
<p>{{blog.content|raw}}</p>
</div>
{% endblock %}